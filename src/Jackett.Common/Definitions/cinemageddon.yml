---
  site: cinemageddon
  name: Cinemageddon
  description: "B-movie tracker"
  language: en-us
  type: private
  encoding: UTF-8
  links:
    - https://cinemageddon.net/
  legacylinks:
    - http://cinemageddon.net/

  caps:
    categorymappings:
      - {id: 1, cat: Movies, desc: "Action"}
      - {id: 2, cat: Movies, desc: "Horror"}
      - {id: 3, cat: Movies, desc: "Martial Arts"}
      - {id: 4, cat: Movies, desc: "Comedy"}
      - {id: 5, cat: Movies, desc: "Other"}
      - {id: 6, cat: Movies, desc: "Hidden Gems"}
      - {id: 7, cat: Movies, desc: "Sci-Fi"}
      - {id: 8, cat: Movies, desc: "Gore"}
      - {id: 9, cat: Movies, desc: "Exploitation"}
      - {id: 11, cat: Movies, desc: "OST"}
      - {id: 12, cat: Movies, desc: "XXX"}
      - {id: 13, cat: Movies, desc: "Thriller"}
      - {id: 14, cat: Movies, desc: "Adventure"}
      - {id: 15, cat: Movies, desc: "Documentary"}
      - {id: 16, cat: Movies, desc: "Western"}
      - {id: 17, cat: Movies, desc: "Family"}
      - {id: 18, cat: Movies, desc: "Drama"}
      - {id: 19, cat: Movies, desc: "Ebooks"}
      - {id: 20, cat: Movies, desc: "Softcore"}
      - {id: 21, cat: Movies, desc: "Tinfoil Hat"}
      - {id: 22, cat: Movies, desc: "Trailers"}

    modes:
      search: [q, imdbid]
      movie-search: [q, imdbid]

  login:
    path: takelogin.php
    method: post
    inputs:
      username: "{{ .Config.username }}"
      password: "{{ .Config.password }}"
    error:
      - selector: table:contains("Login failed!")
    test:
      path: index.php

  search:
    paths:
      - path: browse.php
    inputs:
      $raw: "{{range .Categories}}c{{.}}=1&{{end}}"
      search: "{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{else}}{{ .Query.Keywords }}{{end}}"
    rows:
      selector: table.torrenttable > tbody > tr:has(a[href^="browse.php?cat="])
    fields:
      category:
        selector: a[href^="browse.php?cat="]
        attribute: href
        filters:
          - name: querystring
            args: cat
      title:
        selector: td:nth-child(2)
      download:
        selector: a[href^="details.php?id="]
        attribute: href
        filters:
          - name: replace
            args: ["details.php?id=", "download.php?id="]
      details:
        selector: a[href^="details.php?id="]
        attribute: href
      imdb:
        optional: true
        selector: a[href*="www.imdb.com/title/"]
        attribute: href
      grabs:
        selector: td:nth-child(6)
      files:
        selector: td:nth-child(5)
        filters:
          - name: regexp
            args: (\d+)\s+file
      size:
        selector: td:nth-child(5)
        filters:
          - name: regexp
            args: (\d+.*(MB|GB)+)
      seeders:
        selector: td:nth-child(7)
      leechers:
        selector: td:nth-child(8)
      date:
        selector: td:nth-child(4)
        filters:
          - name: regexp
            args: (\d{4}-\d{2}-\d{2})
      # processing all kinds of up/dl bonus probably needs C#
      # types of bonuses:
      # - 4 types of coins (20,30,40,100%) (there's also black coin, but I don't think it's being used) https://cinemageddon.net/help/newuser.php
      # - featured torrents (50%) on browse page
      # - bumpy bonus (1%+) http://cinemageddon.net/cocks/showpage.php?id=368 - problematic one,
      # since it can be applied to torrents already having other bonuses and only the highest one is counted
